{
  "name": "@voxelspy/session-archive",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "files": [
    "dist",
    "README.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "browser:check": "node scripts/check-browser.mjs",
    "check": "pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm browser:check",
    "lint": "node scripts/check-boundaries.mjs",
    "test": "vitest run",
    "typecheck": "pnpm --filter @voxelspy/contracts build && tsc -p tsconfig.json"
  },
  "dependencies": {
    "@voxelspy/contracts": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "vitest": "3.2.7"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@10.33.2"
}
